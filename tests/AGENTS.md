Tests Folder

Purpose
- GDScript test runner and simple tests for path validity and economy rules.

Notes
- Tests are run via Godot. Use: `godot --headless --script res://tests/run_tests.gd`.
- Codex sandbox cannot run Godot headless here; run locally.
